const app = require('express')();

const assert = require('assert');
describe('Simple Math Test', () => {
 
 it('should return 9', () => {
        assert.equal(3 * 3, 9);
    });

    it('should return 9', () => {
        assert.equal(3 * 3, 9);
    });
});

//ssssss